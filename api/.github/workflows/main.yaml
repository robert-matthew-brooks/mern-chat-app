name: Run Jest test on push

on: [push]

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2

      - name: Install modules
        run: npm install

      - name: Run tests
        run: npm test
